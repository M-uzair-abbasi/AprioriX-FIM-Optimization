# Core scientific computing
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Notebook environment
jupyter>=1.0.0
ipykernel>=6.20.0
ipywidgets>=8.0.0

# Memory profiling (standard library, listed for completeness)
# tracemalloc — built into Python 3.12
# gc — built into Python 3.12

# Optional: progress bars in notebooks
tqdm>=4.65.0

# Optional: psutil for additional memory monitoring
psutil>=5.9.0
